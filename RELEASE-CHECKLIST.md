# 送審前檢查表

商店審查要幾天，過了才發現不能用就是再等一輪。這張表存在的理由很具體：0.4.3 送審前沒人實際點過「自己開房等對手」，而那正是這個擴充最主要的用途——面板在那個情境下完全認不出房間，卻通過了所有自動檢查。

**每一項都要在載入未封裝版之後、真的用滑鼠點過。** 語法檢查與 API 測試擋不住這類問題：它們錯的地方在「真實情境跟我假設的不一樣」，不在程式碼本身。

## 開始之前

1. `chrome://extensions` → 重新載入這個擴充（載的是資料夾，不是 zip）
2. 確認版本號是你要送審的那一版
3. 重新整理 `play.riftatlas.com` 的分頁

## 一個人就能測

- [ ] **開房等對手**：在 Rift Atlas 建立房間 → 面板的「我的房間」顯示房號（不是「在 Rift Atlas 建立房間之後…」）
- [ ] **掛出**：填稱呼 → 按「掛到布告欄等對手」→ 板上出現，[網站的 /rooms](https://riftbound.chroniclecore.com/rooms) 也看得到
- [ ] **稱呼自動帶入**：第一次掛的時候，稱呼欄預設是你在 Rift Atlas 用的名字
- [ ] **牌組檢查**：選一副只有當前環境的牌 → 顯示「✓ N 張卡都在⋯以內」；換一副混入舊系列的 → 顯示超出的那幾張
- [ ] **收掉**：按「收掉」→ 板上消失
- [ ] **離開房間**：掛著房間時離開 Rift Atlas 的房間 → 板上那間自動消失（可能要等幾秒）
- [ ] **關掉分頁**：掛著房間時直接關掉分頁 → 板上那間消失
- [ ] **英文介面**：Chrome 設定改成英文（或用另一個語系的 profile）→ 面板文字變英文、擴充名稱變 Rift Atlas Companion

## 需要一個幫手

- [ ] **對手加入**：對方進你的房間 → 提示音響一聲，板上那間從「正在等對手」移到「進行中，可觀戰」
- [ ] **觀戰**：第三個人點板上「進行中」那組的「觀戰」→ 進得去，而且他的面板**不會**把那間房當成自己的（「我的房間」應該是空的）
- [ ] **加入**：對方點板上「正在等對手」那組的「加入」→ 直接進到你的房間

## 異常情境

- [ ] **權限被關掉**：`chrome://extensions` → 這個擴充 → 詳細資料 → 網站存取權改成「在點選時」→ 回去按掛出 → 應該顯示「這個擴充還沒有存取編年史的權限」加一顆「開啟權限設定」按鈕（不是英文的 Failed to fetch）。測完記得改回「在所有網站上」
- [ ] **斷網**：關掉網路 → 按掛出 → 顯示「連不上編年史，檢查一下網路再試」

## 送審

- [ ] `manifest.json` 的 version 比架上那版新
- [ ] 打包的 zip 是**剛才測過的那份程式碼**（重新打包，別用舊的 zip）
- [ ] 商店列表的截圖與說明是否需要跟著更新
